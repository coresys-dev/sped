[workspace]
resolver = "2"
members = [
    "src-tauri",
    "crates/device",
    "crates/mapping",
    "crates/integrations",
]

[workspace.package]
edition = "2021"
license = "MIT"

[profile.release]
codegen-units = 1
lto = true
opt-level = 3
panic = "abort"
strip = true
